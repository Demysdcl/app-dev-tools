import { createContext } from 'react';

export interface {{pascalCase name}}ContextProps {
  state: string;
  setState: (state: string) => void;
}

const INITIAL_STATE: {{pascalCase name}}ContextProps = {
  state: '',
  setState: () => null,
};

export const {{pascalCase name}}Context = createContext(INITIAL_STATE);
