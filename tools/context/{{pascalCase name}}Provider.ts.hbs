import React, { useMemo, useState, ReactElement } from 'react';

import {
  {{pascalCase name}}Context,
  {{pascalCase name}}ContextProps,
} from './{{pascalCase name}}Context';

interface {{pascalCase name}}ProviderProps {
  children: ReactElement;
};

export const {{pascalCase name}}Provider = ({
  children,
}: {{pascalCase name}}ProviderProps) => {
  const [state, setState] = useState('');

  const state = useMemo<{{pascalCase name}}ContextProps>(
    () => ({
      state,
      setState
    }),
    [state, setState]
  );

  return (
    <{{pascalCase name}}Context.Provider value={state}>
      {children}
    </{{pascalCase name}}Context.Provider>
  );
};
