import { styled, spacingNames } from '@contasimples/simples-react-native-ui';

const { xxs } = spacingNames;

export const {{pascalCase name}}Container = styled.View`
  flex: 1;
  padding: ${({ theme }) => theme.spacing(xxs)};
  background-color: ${({ theme }) => theme.palette.neutral.lighter};
`;
